language: node_js

env : 
- ASCIIDOCTOR_CORE_VERSION=v2.0.10
- ASCIIDOCTOR_CORE_VERSION=master

node_js:
- 'lts/*' #derniere version lts de node distribué
- 'node' #derniere version stable de node distribué

os:
- linux
- osx

jdk:
- oraclejdk8

install:
- npm ci
- npmm ci --prefix packages/core

script:
- npm run lint
- npm run test
- npm run travis --prefix packages/core
  #yarn test -> alternatuve de npm test
  #- sudo npm install
  #pariel que install ?
  #- sudo npm run test